jquery.geocodable.js
version 0.9

First cut at a jquery plugin to mark one or more text fields as 'geocodable',
causing a geocoding lookup to occur onBlur (optional) or onSubmit
(not optional in this version).
